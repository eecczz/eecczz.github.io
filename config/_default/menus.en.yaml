# Navigation Links
#   To link a homepage widget, specify the URL as a hash `#` followed by the filename of the
  #     desired widget in your `content/home/` folder.
  #   The weight parameter defines the order that the links will appear in.

main:
  - identifier: "home"
    name: "home"  # i18n 파일에서 'menu.home' 키를 사용
    url: "/"
    weight: 10

  - identifier: "about"
    name: "about"
    url: "/about/"
    weight: 20

  - identifier: "contact"
    name: "contact"
    url: "/contact/"
    weight: 30

    pre: '<i class="fas fa-globe"></i>'  # 아이콘만 표시
    children:
      - name: "English"
        url: "/en/"
        identifier: "en"
      - name: "한국어"
        url: "/ko/"
        identifier: "ko"
